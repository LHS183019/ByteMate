# 单元测试实现总结

## 概览
已使用 **Jest** 和 **JSDOM** 为 ByteMate 扩展建立了单元测试。测试基础设施模拟了 Chrome API（`chrome.runtime`、`chrome.storage`、`chrome.tabs`）以仿真扩展运行环境。

项目已配置 **代码覆盖率 (Code Coverage)** 检测，并在 CI/CD 流程中自动生成和上传覆盖率报告。

## 已实现的测试

### 1. 弹出窗口 (`tests/popup.test.js`)
测试覆盖了扩展弹出界面的功能：
- **设置加载**：验证选定的 LLM 模型、**目标语言**和 API Key 是否能从存储中正确加载并填充到 UI 中。
- **统计显示**：检查每日学习统计数据（解决题目数、辅助次数、标签数）是否被正确获取并显示。
- **设置持久化**：
  - 确保点击“保存”按钮能正确存储用户的 API Key 和模型选择。
  - **语言切换**：验证切换目标语言（C++/Python）时，配置能正确保存并通知后台。
  - 在保存前验证 API Key 是否符合长度要求。
  - 验证空 API Key 的移除逻辑。
- **交互逻辑**：
  - **密码显示切换**：验证点击眼睛图标是否能切换 API Key 输入框的类型（password/text）。
  - **模型切换事件**：验证切换模型下拉框时，UI 状态（如 API Key 输入框的显示/隐藏）是否正确更新。
- **导航**：验证“仪表板”按钮是否能正确触发打开仪表板页面的操作。

### 2. 内容脚本 (`tests/content.test.js`)
测试覆盖了注入到网页中的交互元素及核心逻辑。为了支持测试，`UIManager` 和 `LearningTracker` 类已被暴露给 `window` 对象。

- **UI 组件与交互**：
  - **侧边栏生命周期**：验证侧边栏 DOM 元素的创建、追加及可见性切换。
  - **用户交互**：验证关闭/折叠按钮、重试/重新加载按钮的功能。
  - **代码块增强**：验证 `addCopyButtons` 是否正确为代码块添加复制按钮。
- **核心逻辑**：
  - **Markdown 解析**：验证 `parseMarkdown` 函数能否正确处理粗体、代码块、行内代码和列表。
  - **页面类型检测**：验证 `getCurrentPageType` 能否根据 URL 正确识别 `problem` (题目页), `submit` (提交页), `result` (结果页) 等类型。
  - **题目解析**：验证 `parseProblemFromDocument` 能否从 DOM 中正确提取题目标题、描述、输入输出样例等信息。
- **学习追踪 (`LearningTracker`)**：
  - **题目 ID 提取**：验证从 URL 或页面标题中提取题目 ID 的逻辑。
  - **数据上报**：验证题目完成 (`recordProblemSolved`) 和尝试 (`recordAttempt`) 时是否正确发送了 Chrome 消息。
- **反馈机制**：
  - **UI 显示**：验证内容生成后反馈按钮（“我不满意”）是否正确出现。
  - **触发重生成**：模拟点击反馈选项，验证是否发送了带有反馈原因的重生成请求。

### 3. 后台服务 (`tests/background.test.js`)
测试覆盖了 Service Worker 的核心逻辑，包括配置管理、消息路由和 LLM 调用。

- **配置管理**：
  - **初始化**：验证从 `chrome.storage` 加载模型配置和 API Key。
  - **更新**：验证 `update_config` 消息能正确更新内存中的配置。
- **消息处理**：
  - **路由**：验证 `onMessage` 监听器能正确分发 `ping`, `open_url`, `copy_code` 等基础消息。
  - **缓存**：验证题目信息的缓存 (`cache_problem`) 和读取 (`get_cached_problem`)。
  - **遥测**：验证 `record_problem_solved` 等消息能触发正确的存储更新。
- **AI 功能**：
  - **Prompt 生成**：验证 `generatePrompt` 能根据题目上下文、用户代码和**目标语言配置**生成正确的提示词（包含正确的语言标记和指令）。
  - **功能调用**：验证 `invoke_feature` 能正确组装请求并调用 LLM API（通过 Mock Fetch）。

## 运行测试
执行测试套件：
```bash
npm test
```
当前状态：**51/51 测试通过** (2025-12-18)

## 测试覆盖详情

| 模块 | 语句覆盖率 | 备注 |
| :--- | :--- | :--- |
| **Popup** | ~73% | UI 交互逻辑覆盖较全 |
| **Content** | ~50% | 核心解析逻辑已覆盖 |
| **Background** | ~50% | 消息处理与核心流程已覆盖 |

## CI/CD 集成
GitHub Actions 工作流 (`test.yml`) 已配置：
1.  在每次 Push 和 PR 时运行测试。
2.  生成代码覆盖率报告。
3.  将覆盖率报告作为 Artifact (`coverage-report`) 上传，可在 Actions 页面下载查看。

## 测试覆盖详情表

| 功能 | 测试状态 | 备注 |
| :--- | :--- | :--- |
| **PopUp (弹出窗口)** | | |
| 切换 LLM 供应商 | ✅ 通过 | 验证模型切换及 UI 更新 |
| 切换目标语言 | ✅ 通过 | 验证语言设置保存及通知 |
| 输入/保存 API Key | ✅ 通过 | 验证保存逻辑及格式校验 |
| 密码显示切换 | ✅ 通过 | 验证 input type 切换 |
| 打开仪表板 | ✅ 通过 | 验证 `chrome.runtime.getURL` 调用 |
| **Content Script (内容脚本)** | | |
| 侧边栏创建与交互 | ✅ 通过 | 验证 DOM 结构及事件绑定 |
| Markdown 解析 | ✅ 通过 | 验证文本到 HTML 的转换 |
| 题目信息提取 | ✅ 通过 | 验证从 DOM 提取题目元数据 |
| 页面类型识别 | ✅ 通过 | 验证 URL 路由逻辑 |
| 学习行为追踪 | ✅ 通过 | 验证题目 ID 提取及消息发送 |
| 代码块复制按钮 | ✅ 通过 | 验证 DOM 操作 |
| **Background Service (后台服务)** | | |
| 配置初始化 | ✅ 通过 | 验证从 Storage 加载配置 |
| Prompt 生成 | ✅ 通过 | 验证多语言支持及上下文注入 |
| 消息路由 | ✅ 通过 | 验证各类型消息的分发 |
| 遥测上报 | ✅ 通过 | 验证参数携带 (含 target_language) |

